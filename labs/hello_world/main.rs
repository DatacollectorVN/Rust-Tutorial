// ref: https://doc.rust-lang.org/book/ch01-02-hello-world.html

fn main() {
    // using a ! means that you’re calling a macro instead of a normal function and that macros don’t
    // always follow the same rules as functions.
    println!("Hello world");
}